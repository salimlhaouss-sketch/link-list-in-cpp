#include <stdio.h>
#include <stdlib.h>

int compare(const void *a, const void *b)
{
    return (*(int*)b - *(int*)a); // من الكبير للصغير
}

#include <stdio.h>
#include <stdlib.h>

int compare(const void *a, const void *b)
{
    return (*(int*)b - *(int*)ص
int main()
{
    int T[] = {1,3,2,4,6,5};
    int n = sizeof(T)/sizeof(T[0]);
    int i;

    qsort(T, n, sizeof(int), compare);

    for(i = 0; i < n; i++)
    {
        printf("%d ", T[i]);
    }

    return 0;
}}

